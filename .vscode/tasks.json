{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "c program Security HW3",
            "type": "shell",
            "command": "source",
            "args": [
                "task.sh"
            ],
            "options": {
                "cwd": "${workspaceFolder}/.vscode"
            },
            "group": "build",
            "detail": "build"
        }
    ]
}